type Query {
  users(offset: Int = 0, limit: Int = 10): UsersResults
  cities(name: String!): City
}

type UsersResults {
  first_name: String
  last_name: String
  age: Int
  cities: [City]
}

type City {
  city_name: String
  country_name: String
  date_visited: Date
}
